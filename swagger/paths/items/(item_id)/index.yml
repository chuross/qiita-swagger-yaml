get:
  tags: ['Items']
  parameters:
    - name: item_id
      type: string
      in: path
      required: true
  responses:
    200:
      description: ''
      schema:
        $ref: '#/definitions/Item'
