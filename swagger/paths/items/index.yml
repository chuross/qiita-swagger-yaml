get:
  tags: ['Items']
  parameters:
    - name: query
      type: string
      in: query
    - $ref: '#/parameters/page'
    - $ref: '#/parameters/per_page'
  responses:
    200:
      description: ''
      schema:
        type: array
        items:
          $ref: '#/definitions/Item'
