get:
  tags: ['Items']
  parameters:
    - name: user_id
      type: string
      in: path
      required: true
    - $ref: '#/parameters/page'
    - $ref: '#/parameters/per_page'
  responses:
    200:
      description: ''
      schema:
        type: array
        items:
          $ref: '#/definitions/Item'
