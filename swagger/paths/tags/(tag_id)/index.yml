get:
  tags: ['Tags']
  parameters:
    - name: tag_id
      type: string
      in: path
      required: true
  responses:
    200:
      description: ''
      schema:
        $ref: '#/definitions/Tag'
